<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nepobabies.retas">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Retas"
        tools:targetApi="31">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Flow -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.SignUpActivity"
            android:exported="false" />

        <!-- Main App -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- Donate Feature -->
        <activity
            android:name=".ui.donate.DonateDetailsActivity"
            android:exported="false" />

        <!-- Calculator Feature -->
        <activity
            android:name=".ui.calculator.FashionCalculatorActivity"
            android:exported="false" />
        <activity
            android:name=".ui.calculator.ResultActivity"
            android:exported="false" />
<<<<<<< HEAD

=======
        <activity android:name=".ResultActivity" />
>>>>>>> c3742fea21f65b41ff228c29ab08b043c898c607
    </application>

</manifest>